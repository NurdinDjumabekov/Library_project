import { createSlice } from "@reduxjs/toolkit";

const initialState = {};
const usersStateSlice = createSlice({
  name: "usersStateSlice",
  initialState,
  reducers: {},
});

export const {} = usersStateSlice.actions;
export default usersStateSlice.reducer;
