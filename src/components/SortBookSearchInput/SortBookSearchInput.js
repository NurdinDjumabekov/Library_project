import React from "react";
import styles from "./SortBookSearchInput.module.css";

const SortBookSearchInput = () => {
  return (
    <>
      <input type="text" placeholder="Фильтр по названию" />
    </>
  );
};

export default SortBookSearchInput;
